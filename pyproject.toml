[project]
name = "lazy-slide"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.0.1",
    "lazyslide>=0.9.1",
    "wsidata>=0.7.5",
]

[dependency-groups]
dev = [
    "ipykernel>=7.0.1",
]
